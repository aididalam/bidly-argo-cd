apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: auction-frontend
  namespace: bidly
spec:
  hosts:
    - auction-frontend
  http:
    - route:
        - destination:
            host: auction-frontend
            subset: v9
          weight: 100
        - destination:
            host: auction-frontend
            subset: v10
          weight: 0
