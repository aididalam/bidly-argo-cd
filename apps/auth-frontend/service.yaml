apiVersion: v1
kind: Service
metadata:
  name: auth-frontend
  namespace: bidly
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: auth-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30173
